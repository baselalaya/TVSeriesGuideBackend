<!DOCTYPE html>
<html>
<head>
  <title>Edit Series</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    input, textarea { width: 100%; margin: 10px 0; padding: 8px; }
    button { background-color: #007AFF; color: white; padding: 10px; border: none; border-radius: 5px; }
  </style>
</head>
<body>
  <h1>Edit Series</h1>
  <form method="POST">
    <input type="text" name="title" value="<%= series.title %>" required><br>
    <textarea name="description" required><%= series.description %></textarea><br>
    <input type="text" name="channels" value="<%= series.channels.map(ch => `${ch.name}:${ch.time}`).join(', ') %>" required><br>
    <button type="submit">Update Series</button>
  </form>
  <a href="/admin/dashboard">Back</a>
</body>
</html>